# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# PDF processing
pymupdf>=1.24.0          # fitz — PDF to image, faster than pdf2image
Pillow>=10.3.0           # image manipulation

# Vision AI providers (pick one or both for fallback)
google-generativeai>=0.7.0    # Gemini
anthropic>=0.28.0             # Claude (recommended for technical drawings)

# Env & config
python-dotenv>=1.0.0

# Data & validation
pydantic>=2.7.0
pandas>=2.2.0
openpyxl>=3.1.0          # Excel export

# DXF/DWG parsing (rule-based layer)
ezdxf>=1.3.0

# Utilities
httpx>=0.27.0
tenacity>=8.3.0          # retry logic for API calls
